import { useState } from "react";
import { Modal, Button } from "react-bootstrap";

const CarModal = ({car, show, handleClose}) => {
    
    return (
      <carModal>
      </carModal>
    );
};

export default CarModal;